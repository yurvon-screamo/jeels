# Контент-план Origa docs+blog — v3 FINAL (полный scope аудита @reviewer)

Дата: 2026-08-19. Основание: финальный аудит @reviewer (7 фактических ошибок, 6 дыр покрытия, 5 несогласованностей) + обе правки-блокера по v2. Ключевые клеймы перепроверены @marketer по коду в этой сессии (см. `.factcheck.json`).

Точные списки точек замены для счётчика фраз и блог-оверклеймов — в `replacements.md` рядом (этот файл содержит только копирайт).

Всё ниже — paste-ready копирайт. Правки вида «существующее → новое». Порядок = приоритет.

---

## P1-1. Новая статья `content/docs/ru/fsrs.md` (RU; EN зеркально тем же PR)

```
---
title: "Как Origa решает, что вам показать"
slug: /docs/fsrs
locale: ru
meta_title: "Как Origa решает, что вам показать — FSRS простыми словами"
meta_description: "Кривая забывания, интервальное повторение и FSRS: почему Origa ограничивает новые карточки в день, зачем две кнопки оценки вместо четырёх, и откуда берётся расписание повторений."
target_keywords: ["fsrs японский", "интервальное повторение японский", "почему заканчиваются карточки"]
lastmod: 2026-08-19
status: ready
---

# Как Origa решает, что вам показать

Вы отвечаете на карточку — Origa решает, когда показать её снова. Эта страница о том, как принимается это решение: что такое кривая забывания, что такое интервальное повторение, и почему дневной лимит новых карточек — часть метода, а не недоработка.

## Кривая забывания

Выучив слово сегодня, через день вы помните его почти наверняка, через неделю — с трудом, через месяц — вряд ли. Эббингауз измерил эту закономерность ещё в XIX веке; с тех пор форма кривой подтверждена множеством экспериментов. Ключевой факт: память угасает предсказуемо, а значит, момент «почти забыл» можно вычислить заранее.

## Интервальное повторение

Идея — показывать карточку не каждый день, а в момент, когда вы вот-вот её забудете. Повторение в этот момент — самый сильный сигнал для памяти, и каждое успешное напоминание делает следующий интервал длиннее: день, потом неделя, потом месяц. Так вместо равномерного листания колоды вы повторяете мало и вовремя.

## FSRS

FSRS (Free Spaced Repetition Scheduler) — современный алгоритм планирования повторений, с 2023 года — стандарт индустрии SRS-приложений, с 2024 встроен по умолчанию в Anki. По каждому ответу он обновляет модель вашей памяти по этой карточке: насколько прочно вы её знаете и как быстро забываете. Из модели следует интервал до следующего показа. Origa использует FSRS как единственный планировщик: он решает, что показывать сегодня, а что — через месяц.

## Почему две кнопки, а не четыре

В Anki после каждого ответа вы выбираете из четырёх кнопок: «снова», «трудно», «хорошо», «легко». Origa спрашивает одно: **знали вы карточку или нет**. Это осознанное решение, а не упрощение ради упрощения. Промежуточные оценки на практике используются бессмысленно: в Anki «трудно» — самая misuse'имая кнопка, и её нажатие вместо «снова» искажает параметры модели. Для японского ответ чаще всего бинарный: либо вспомнил чтение и значение, либо нет. Меньше времени на выбор — больше времени на язык.

## Почему есть дневной лимит новых карточек

30 новых карточек сегодня — это не 30 карточек сегодня. Это ещё около сотни повторений в ближайшие две недели, которые FSRS распланирует из этих новых. Лимит новых карточек удерживает ежедневный объём повторений в пределах, где вы реально их делаете, а не копите долг. Перегруженная колода возвращается лавиной: карточки, показанные «в долг», возвращаются чаще, и объём растёт быстрее, чем вы успеваете. Отсюда темпы: их можно менять, но у каждого следующего темпа своя цена в минутах в день.

## Темпы и размеры урока

Количество новых карточек в день задаёт темп в настройках профиля. Шесть темпов:

| Темп | Новых карточек в день |
|---|---|
| Минимальный | 3 |
| Лёгкий | 6 |
| Сбалансированный | 9 |
| Сложный | 15 |
| Максимальный | 21 |
| Безумный | 30 |



Урок собирается из новых карточек дня и карточек, подошедших по сроку повторения, — до 22 карточек. Когда и новые, и повторения исчерпаны, Origa сообщает «Нет карточек для изучения» — это не ошибка и не лимит аккаунта: завтра FSRS запланирует новые повторения, и уроки появятся снова.

## По теме

- [Уроки](/ru/docs/lesson)
- [Начало работы](/ru/docs/getting-started)
```

EN-версия — зеркальный перевод, slug `/docs/fsrs`, ключи `fsrs spaced repetition`, `why flashcards run out`.

## P1-2. `lesson.md` — две замены

**a) Темповая секция v2 → компакт + ссылка на /docs/fsrs.** Таблица темпов живёт в /docs/fsrs; дублировать числа в двух местах — плодить дрейф.

> ## Темп и размер урока
>
> Количество новых карточек в день задаёт темп в настройках профиля — шесть значений от 3 до 30. Урок собирается из новых карточек дня и подошедших по сроку повторений, до 22 карточек. Как Origa планирует повторения и зачем нужен дневной лимит — на отдельной странице: [Как Origa решает, что вам показать](/ru/docs/fsrs).
>
> **Уроки закончились, а вы готовы продолжать.** Это значит, что дневная квота новых карточек исчерпана, а повторений по сроку нет. На следующий день FSRS запланирует новые повторения, и уроки появятся снова. Если объёма не хватает — поднимите темп в профиле или заведите набор следующего уровня (см. «Прогресс по JLPT»).

**b) «Прогресс по JLPT» — снятие неточности (блокер v2).** Строку «при подборе материала старается закрыть эти пробелы в первую очередь» заменить на:

> Origa показывает наименьший уровень JLPT, в котором у вас остались пробелы. Новые карточки выдаются по возрастанию уровня (N5 → N1) для всех одинаково — диагностики «где именно у вас дыры» приложение не делает. Индикатор уровня — отражение вашего материала, а не его маршрут.

## P1-3. `getting-started.md` §3 «Уровень» — семантика «цель», не «текущий»

Существующее: «Укажите ваш текущий уровень JLPT (от N5 до N1 или «не знаю»). Если вы уже знаете материал ниже этого уровня, Origa отметит его как известное…» — заменить на:

> **Уровень.** Укажите уровень JLPT, которого вы хотите достичь (от N5 до N1 или «не знаю»). Origa импортирует наборы всех уровней от N5 до вашей цели. Знакомое можно отметить позже, на шаге оценки, — и не учить его с нуля.

## P1-4. `kanji.md` — «Как кандзи попадает в коллекцию»

Переписать секцию: прямой путь первым пунктом (модалка «Добавить кандзи», поиск по кандзи/чтению/описанию — `add_kanji_modal.rs:70-119`), косвенные — как автоматический механизм. Копирайт:

> ## Как кандзи попадает в коллекцию
>
> Прямой способ: откройте справочник кандзи и добавьте нужное через «Добавить кандзи» — поиск работает по самому кандзи, чтению и описанию.
>
> Автоматический путь работает сам: созданные карточки слов несут свои кандзи с собой, и по мере изучения слов справочник кандзи заполняется без вашего участия. Напрямую кандзи обычно добавляют, когда слово уже выучено, а знак нужен отдельно — например, для практики написания.

## P1-5. `limitations.md` — новая секция

> ## Нельзя сбросить прогресс и вернуться в настройку первого запуска
>
> Origa не умеет сбрасывать прогресс и не позволяет пройти настройку первого запуска повторно — после завершения она закрывается. Ошиблись с уровнем на старте? Это исправимо без сброса: уровень на главном экране — не настройка, а расчёт по вашим карточкам. Заведите стандартные наборы уровней, которые вы уже знаете, и отметьте знакомое как изученное — уровень пересчитается сам (подробнее: [Уроки](/ru/docs/lesson)). Полное очищение прогресса — удаление аккаунта в настройках профиля с созданием нового.

## P1-6. Счётчик фраз 200 000+ → 156 000+

Точное число: 156 228 (`cdn/phrases/phrase_index.json`, total). Формат числа — по канону каждой локали. Полный список точек (лендинг ru/en/ko/vi ×3 поля, README ×2, блог best-app ×4 локали) — в `replacements.md`, раздел 1. При переносе в PR сверить каждую точку grep'ом `200` по файлам.

## P1-7. Блог — три оверклейма (Н1–Н3), все локали

**Н1. Anki: история повторений НЕ переносится.** Точные строки по всем локалям — в `replacements.md`, раздел 2. Замена (RU; остальные — зеркально):

> Можно импортировать готовые колоды Anki (`.anki2`, `.anki21`, `.anki21b`) — переносится список слов, Origa создаёт карточки заново. История повторений и интервалы не переносятся: карточки начнутся как новые. Для колод с многолетней историей это заметная потеря — учитывайте это при миграции.

`learn-japanese-from-manga.md:71` (RU): «миграция односторонняя и без потерь» → «миграция односторонняя; история повторений не переносится — карточки начнутся как новые».

**Н2. «FSRS настраивается по колодам» → честно.** `anki-alternative-japanese.md`, таблица «Как Origa справляется» (RU + EN/VI/KO):

> Origa использует FSRS с фиксированными параметрами под каждый тип карточек (слова, фразы, грамматика, кандзи). Пользовательской настройки параметров FSRS нет: значения подобраны под каждый тип контента. FSRS не наша разработка — Origa его использует, а не изобретает.

**Н3. «Предложение, в котором слово встретилось» — удалить приписывание Origa.** Точки: `blog/ru/japanese-ocr-app.md:61`, `blog/ko/japanese-ocr-app.md:61`, `blog/ru/japanese-ocr-app.md:79` и аналоги (полный список — `replacements.md`, раздел 3). Замена (RU):

> каждое становится карточкой с чтением, переводом и аудио. Изображение не сохраняется.

Внимание: `learn-japanese-from-manga.md:50` «Добавьте каждое в SRS с предложением…» — общий совет по майнингу, НЕ утверждение о Origa; не трогать.

---

## P2 (отдельным PR после P1)

- **B2:** getting-started: дописать шаги онбординга Прогресс/Сводка/Оценка; ключ — «Оценка» как механизм «не учить с нуля».
- **C1:** getting-started §6 — устранить противоречие про интернет («только для синхронизации» vs «проверить обновления, импортировать подборки»).
- **C2:** «Офлайн-пакет» → «Оффлайн-пакет» во всех RU docs (как в UI).
- **C3 (готово, числа от @developer, проверены @marketer по коду):** capture.md — заменить «действует ограничение на размер файла» на конкретику: «Изображение для распознавания — до 10 МБ, аудиозапись — до 50 МБ» (`ocr_processing.rs:32` MAX_FILE_SIZE_MB=10.0; `audio_input_stage.rs` max_size_mb=50.0; строки `file_too_large` в ru.json:409,439). EN: "Images up to 10 MB, audio recordings up to 50 MB."
- **C4:** index.md: добавить ссылку на release notes.
- **A3:** UI-copy темпов (переименование Heavy/Maximum + load_heavy_desc) — отдельный тикет @developer.
- **B4:** FAQ лендинга faq_q6/q7 — формулировки из v2 в силе.

## P3 (опционально)

- «По теме» в lesson.md и соседних страницах — добавить ссылку /docs/fsrs.
- Скриншот-иллюстрация кривой забывания для /docs/fsrs (по канону дизайна лендинга).

## Fact-check сводка (подробно — `.factcheck.json`)

Числа и механики верифицированы по коду в этой сессии: темпы 3/6/9/15/21/30 (`value_objects.rs:242-259`), урок ≤22 и тесты ассертят только верхнюю границу (`lesson_builder.rs:11-12`, `tests.rs:1157,1185`), добор padding только high-difficulty (`lesson_builder.rs:267-285`), распределение новых N5→N1 без таргетинга (`lesson_builder.rs:1295-1310`), retention захардкожен 0.95/0.85/0.70/0.90/0.85 (`srs.rs:36-65`), у VocabularyCard нет поля предложения (`vocabulary.rs:16`), Anki читает только notes без revlog (`import_anki_pack.rs:95,184`), фразы = 156 228 (phrase_index.json), онбординг = target_level (ru.json:123 «которого вы хотите достичь»; `import_onboarding_sets.rs:38,83-87`), обоснование двух кнопок — ADR-005 (simplified-rating-system), механика стадий — ADR-004.
